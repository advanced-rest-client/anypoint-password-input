{
  "name": "anypoint-password-input",
  "version": "0.1.1",
  "license": "All rights reserved to Mulesoft Inc.",
  "description": "Anypoint password input with preview password action",
  "devDependencies": {
    "polymer-cli": "^0.16.0"
  },
  "engines": {
    "node": ">=0.10.0"
  },
  "private": false,
  "main": [
    "anypoint-password-input.html"
  ],
  "author": "The Advanced REST client authors <arc@mulesoft.com>",
  "scripts": {
    "serve": "node_modules/.bin/polymer serve",
    "lint": "node_modules/.bin/polymer lint anypoint-password-input.html",
    "test-sauce": "node_modules/.bin/polymer test --plugin sauce --job-name \"anypoint-password-input:local-test\"",
    "test": "node_modules/.bin/polymer test",
    "deps": "npm install --quiet && bower install --quiet",
    "update-deps": "npm update --quiet && bower update --quiet"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/advanced-rest-client/anypoint-password-input.git"
  },
  "bugs": {
    "url": "https://github.com/advanced-rest-client/anypoint-password-input/issues",
    "email": "arc@mulesoft.com"
  }
}
